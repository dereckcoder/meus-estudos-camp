# 👋 Hello World - Múltiplas Linguagens

Um exemplo clássico: "Hello, World!" implementado em diferentes linguagens de programação.

## 📝 Sobre

"Hello, World!" é tradicionalmente o primeiro programa que se escreve ao aprender uma nova linguagem. Este exemplo demonstra a sintaxe básica de várias linguagens populares.

## 🔤 Linguagens Incluídas

### Python
```python
# hello.py
print("Hello, World!")
```

**Executar**: `python hello.py`

### JavaScript (Node.js)
```javascript
// hello.js
console.log("Hello, World!");
```

**Executar**: `node hello.js`

### Java
```java
// Hello.java
public class Hello {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

**Executar**: `javac Hello.java && java Hello`

### C
```c
// hello.c
#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
```

**Executar**: `gcc hello.c -o hello && ./hello`

### C++
```cpp
// hello.cpp
#include <iostream>

int main() {
    std::cout << "Hello, World!" << std::endl;
    return 0;
}
```

**Executar**: `g++ hello.cpp -o hello && ./hello`

### Go
```go
// hello.go
package main

import "fmt"

func main() {
    fmt.Println("Hello, World!")
}
```

**Executar**: `go run hello.go`

### Rust
```rust
// hello.rs
fn main() {
    println!("Hello, World!");
}
```

**Executar**: `rustc hello.rs && ./hello`

### Ruby
```ruby
# hello.rb
puts "Hello, World!"
```

**Executar**: `ruby hello.rb`

## 🎯 O Que Aprender

Observe como cada linguagem:
- Tem sua própria sintaxe
- Requer (ou não) compilação
- Estrutura o código
- Lida com saída de dados

## 📚 Próximos Passos

Depois do "Hello, World!", experimente:
1. Adicionar input do usuário
2. Criar funções
3. Trabalhar com variáveis
4. Implementar estruturas de controle (if, loops)

---

**Dica**: Copie o código de qualquer exemplo acima e experimente modificá-lo!
